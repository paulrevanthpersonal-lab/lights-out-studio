const core=window.LightsCore,$=s=>document.querySelector(s);let state={size:5,board:[],moves:0,seconds:0,timer:null,started:false};
function pattern(size){const count=Math.max(5,size+2),moves=[];for(let i=0;i<count;i++)moves.push([(i*3+1)%size,(i*2+2)%size]);return core.seeded(size,moves);}
function formatTime(seconds){return `${String(Math.floor(seconds/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`;}
function reset(size=state.size){clearInterval(state.timer);state={size,board:pattern(size),moves:0,seconds:0,timer:null,started:false};render();$("#moveLog").innerHTML="<li>Pattern initialized</li>";}
function startClock(){if(state.started)return;state.started=true;state.timer=setInterval(()=>{state.seconds++;$("#timer").textContent=formatTime(state.seconds);},1000);}
function render(){const board=$("#board");board.style.setProperty("--size",state.size);board.innerHTML=state.board.map((on,i)=>`<button class="light ${on?"on":""}" role="gridcell" aria-label="Row ${Math.floor(i/state.size)+1}, column ${i%state.size+1}, ${on?"on":"off"}" aria-pressed="${Boolean(on)}" data-index="${i}"><span></span></button>`).join("");$("#moves").textContent=String(state.moves).padStart(2,"0");$("#timer").textContent=formatTime(state.seconds);$("#best").textContent=localStorage.getItem(`lights-best-${state.size}`)||"—";}
function play(index){startClock();const row=Math.floor(index/state.size),col=index%state.size;state.board=core.toggle(state.board,state.size,row,col);state.moves++;render();const item=document.createElement("li");item.textContent=`R${row+1} · C${col+1} flipped`;$("#moveLog").prepend(item);if(core.solved(state.board)){clearInterval(state.timer);const previous=Number(localStorage.getItem(`lights-best-${state.size}`)||Infinity);if(state.moves<previous)localStorage.setItem(`lights-best-${state.size}`,String(state.moves));$("#winSummary").textContent=`${state.size}×${state.size} board · ${state.moves} moves · ${formatTime(state.seconds)}`;$("#winDialog").showModal();}}
$("#board").addEventListener("click",event=>{const button=event.target.closest("[data-index]");if(button)play(Number(button.dataset.index));});
document.addEventListener("click",event=>{const size=event.target.closest("[data-size]");if(size){document.querySelectorAll("[data-size]").forEach(b=>b.classList.remove("active"));size.classList.add("active");reset(Number(size.dataset.size));}});
$("#newGame").addEventListener("click",()=>reset());$("#playAgain").addEventListener("click",()=>{$("#winDialog").close();reset();});
$("#hint").addEventListener("click",()=>{const hint=core.findHint(state.board,state.size);if(!hint)return;const i=core.index(hint.row,hint.col,state.size),button=document.querySelector(`[data-index="${i}"]`);button.classList.add("hint");setTimeout(()=>button.classList.remove("hint"),900);});
$("#soundToggle").addEventListener("click",event=>{const on=event.currentTarget.getAttribute("aria-pressed")==="true";event.currentTarget.setAttribute("aria-pressed",String(!on));event.currentTarget.textContent=on?"SOUND OFF":"SOUND ON";});reset();
